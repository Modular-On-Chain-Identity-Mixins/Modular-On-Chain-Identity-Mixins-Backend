# cargo-audit configuration (RustSec advisory DB).
# See https://github.com/rustsec/rustsec/tree/main/cargo-audit#configuration

[advisories]
# `paste` (RUSTSEC-2024-0436) is flagged "unmaintained", not vulnerable.
# It is a transitive dependency of ark-ff (soroban-env-host -> soroban-sdk's
# BLS12-381/BN254 pairing stack) with no patched or successor release that
# the SDK can adopt, and no known vulnerability in our usage. Revisit this
# entry whenever the soroban-sdk dependency is upgraded.
ignore = ["RUSTSEC-2024-0436"]
